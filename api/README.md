# ThinkPixelMP API

The canonical OpenAPI 3.1 document is [openapi/thinkpixelmp.yaml](openapi/thinkpixelmp.yaml). Reusable artifact and evidence schemas live in [schemas](schemas/).

The Phase 0 specification describes the intended release-candidate surface. A documented endpoint is not an implementation claim; conformance and drift checks begin in later phases.

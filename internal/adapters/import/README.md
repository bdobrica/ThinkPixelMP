# Import adapters

This is a grouping directory, not a Go package: `import` is a Go keyword. Child packages adapt configured upstream sources into candidate-only marketplace imports. They must use the central hardened fetch and registry boundaries and cannot promote imported content automatically.
